import mountElement from './mountElement.js';
import mountAll from './mountAll.js';
import mountPrepend from './mountPrepend.js';
import mountAppend from './mountAppend.js';

export { mountElement, mountAll, mountPrepend, mountAppend };